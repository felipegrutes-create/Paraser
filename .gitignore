# Configuração local do Claude Code (não faz parte do sistema)
.claude/

# Sessões locais de brainstorming visual (efêmeras)
.superpowers/

# Lixo do SO
Thumbs.db
.DS_Store
desktop.ini

# CSVs com PII de pacientes (LGPD — NUNCA commitar)
leads-meta.csv
leads-meta-brevo.csv
leads-*.csv

# Clone clasp local do "Confirmações Agenda" (fonte versionada é confirmacoes-whatsapp.gs)
apps-script-confirmacoes/
